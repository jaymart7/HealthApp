# Nothing to keep. The one thing R8 could plausibly strip here is TodaySnapshot's generated
# serializer, and `encodeSnapshot`/`decodeSnapshot` name it explicitly rather than reifying it,
# which R8 follows on its own.
