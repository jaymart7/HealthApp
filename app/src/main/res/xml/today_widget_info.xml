<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is here for one reason only: day rollover. Every in-app change is pushed
  immediately by FitPulseApplication's collector, but the today-only repository overloads resolve
  "today" when their flow is built, so a widget session that spans midnight keeps showing
  yesterday until something restarts it. 30 minutes is the platform minimum.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:minWidth="180dp"
    android:minHeight="60dp"
    android:previewImage="@mipmap/ic_launcher"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
